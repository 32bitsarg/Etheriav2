Modal action sprites are generated as transparent PNGs in `public/assets/ui/modal-actions`.

They are intentionally small deterministic raster ornaments so the modal buttons and separators keep clean alpha edges without opaque AI-generated backgrounds.
